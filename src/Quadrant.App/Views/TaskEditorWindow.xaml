<Window x:Class="Quadrant.App.Views.TaskEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="任务编辑"
        Width="560"
        MinWidth="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="{StaticResource WindowMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource TitleMediumTextStyle}" Text="{Binding EditorTitle}" />

        <StackPanel Grid.Row="1" Margin="{StaticResource FieldTopMargin}">
            <TextBlock Style="{StaticResource CaptionTextStyle}" Text="标题（必填）" />
            <TextBox x:Name="TitleBox" Margin="{StaticResource CaptionTopMargin}" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Style="{StaticResource ValidationTextStyle}" Text="{Binding TitleError}" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="{StaticResource FieldTopMargin}">
            <TextBlock Style="{StaticResource CaptionTextStyle}" Text="象限" />
            <ComboBox Margin="{StaticResource CaptionTopMargin}"
                      ItemsSource="{Binding Quadrants}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedValue="{Binding QuadrantId}" />
        </StackPanel>

        <Grid Grid.Row="3" Margin="{StaticResource FieldTopMargin}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="{StaticResource FormColumnGap}" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Style="{StaticResource CaptionTextStyle}" Text="截止日期（可选）" />
                <DatePicker Margin="{StaticResource CaptionTopMargin}" SelectedDate="{Binding DueDate}" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Style="{StaticResource CaptionTextStyle}" Text="截止时间" />
                <ComboBox Margin="{StaticResource CaptionTopMargin}"
                          IsEditable="True"
                          IsTextSearchEnabled="False"
                          ItemsSource="{Binding TimeSuggestions}"
                          Text="{Binding DueTimeText, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Style="{StaticResource ValidationTextStyle}" Text="{Binding DueTimeError}" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="4" Margin="{StaticResource FieldTopMargin}">
            <TextBlock Style="{StaticResource CaptionTextStyle}" Text="备注" />
            <TextBox Margin="{StaticResource CaptionTopMargin}"
                     MinHeight="96"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="{StaticResource FieldTopMargin}" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button IsCancel="True" MinWidth="80" Content="取消" />
            <Button Margin="{StaticResource ButtonGap}" IsDefault="True" IsEnabled="{Binding IsValid}" MinWidth="80" Click="Save_Click" Content="保存" />
        </StackPanel>
    </Grid>
</Window>

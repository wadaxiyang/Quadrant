<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate x:Key="TaskCardTemplate">
        <Border Style="{StaticResource TaskCardStyle}" Margin="{StaticResource TaskCardBottomMargin}">
            <StackPanel>
                <TextBlock Style="{StaticResource BodyStrongTextStyle}"
                           Text="{Binding Title}"
                           TextWrapping="Wrap" />
                <TextBlock Style="{StaticResource CaptionTextStyle}"
                           Margin="{StaticResource CaptionTopMargin}"
                           Opacity="0.72"
                           Text="{Binding DueText}" />
                <TextBlock Style="{StaticResource CaptionTextStyle}"
                           Margin="{StaticResource CaptionTopMargin}"
                           Opacity="0.72"
                           Text="{Binding ReminderText}"
                           Visibility="{Binding ReminderText, Converter={StaticResource StringToVisibilityConverter}}" />
            </StackPanel>
        </Border>
    </DataTemplate>
</ResourceDictionary>

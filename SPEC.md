# Quadrant V1 Product Specification

## 1. 产品定位

**Quadrant** 是一个面向 Windows 的极简四象限任务执行器。

它不是通用生产力平台，也不是完整 GTD 系统。V1 只负责：

1. 快速记录任务；
2. 将任务归入固定 2×2 四象限；
3. 设置截止日期/时间；
4. 设置独立提醒时间；
5. 使用 Windows 原生通知到点提醒；
6. 在通知中完成、延后或打开任务；
7. 完成任务后归档；
8. 在 Today / Overdue / Search 等轻量视图下继续保持四象限认知模型。

### 1.1 产品成功标准

V1 成功不是“功能多”，而是以下五点同时成立：

- **输入快**：从任意 Windows 应用通过全局快捷键快速添加任务；
- **认知稳定**：主界面始终是四象限，不引入复杂导航体系；
- **提醒可靠**：支持 Due 与 Reminder 分离，并尽量利用 Windows 调度；
- **后台安静**：无网络、无后台同步、无周期性轮询；
- **界面成熟**：严格使用固定 Fluent Design System，不让 Agent 自行设计。

---

## 2. V1 功能范围

### 2.1 四象限主界面

固定 2×2 布局，四个位置不可改变：

| 位置 | 默认名称 | 默认副标题 |
|---|---|---|
| Q1 | 重要且紧急 | 立即处理 |
| Q2 | 重要不紧急 | 规划推进 |
| Q3 | 紧急不重要 | 简化或委派 |
| Q4 | 不重要不紧急 | 删除或延后 |

允许用户修改四个象限的：

- 名称；
- 副标题；

V1 不允许增加第五象限、不允许改变 2×2 结构。

### 2.2 任务字段

每个任务只保留以下字段：

- `Id`：数据库主键；
- `Title`：必填；
- `QuadrantId`：1–4；
- `DueAt`：可空，截止日期和时间；
- `ReminderAt`：可空，独立提醒时间；
- `Note`：可空，纯文本；
- `IsCompleted`；
- `CompletedAt`：可空；
- `CreatedAt`；
- `UpdatedAt`。

### 2.3 Due 与 Reminder 必须分离

`DueAt` 与 `ReminderAt` 是两个独立字段。

示例：

- 截止：2026-08-25 18:00
- 提醒：2026-08-23 09:00

禁止通过一个字段同时表达截止和提醒。

### 2.4 新建 / 编辑 / 完成 / 删除

支持：

- 新建任务；
- 编辑任务；
- 标记完成；
- 恢复已完成任务；
- 删除任务；
- 拖拽到其他象限完成重新分类。

删除动作必须有轻量确认，防止误删；完成动作不弹确认。

### 2.5 快速添加

必须支持系统级快捷键，默认建议：`Ctrl + Alt + Q`。

快捷键冲突时：

- 不允许崩溃；
- 显示可理解错误；
- 用户可在设置中改为其他组合键。

Quick Add 窗口至少包括：

- 任务标题；
- 象限；
- 可选展开的截止/提醒；

默认流程必须允许：输入标题 → 选择象限 → Enter 保存。

### 2.6 截止时间

支持：

- 无截止时间；
- 日期；
- 日期 + 时间。

WPF 原生没有标准 TimePicker。V1 使用：

- 原生 `DatePicker`；
- 可编辑时间 `ComboBox`，默认提供 15 分钟间隔候选；
- 同时允许用户输入 `H:mm` / `HH:mm`；
- 保存前统一解析并验证。

### 2.7 提醒

提醒预设：

- 不提醒；
- 到期时；
- 提前 10 分钟；
- 提前 1 小时；
- 提前 1 天；
- 自定义。

当任务无 `DueAt` 时，仍允许自定义 `ReminderAt`。

### 2.8 Windows 原生通知

本地通知必须使用 Windows 官方通知 API，不自绘“伪通知窗口”。

通知显示至少包含：

- 任务标题；
- 截止信息（若有）；
- 操作按钮：`完成`、`延后 10 分钟`、`打开`。

行为：

- `完成`：后台标记任务完成，不强制弹主窗口；
- `延后 10 分钟`：把 `ReminderAt` 更新为当前时间 + 10 分钟并重新调度；
- `打开`：激活应用并打开该任务编辑器。

### 2.9 Scheduled Reminder

优先使用 Windows Scheduled App Notification，而不是应用内部每秒/每分钟轮询。

已知约束：Scheduled Notification 的官方投递窗口约 5 分钟。电脑关机或睡眠错过后，V1 不声称“绝对保证补发”。

V1 的补救策略：

- 应用启动/恢复时查询数据库中 `ReminderAt < Now` 且未完成的任务；
- 在应用内显示“可能错过的提醒”提示区；
- 不无条件重复弹系统 Toast，避免用户已经看过/关闭过通知后再次骚扰。

### 2.10 Today / Overdue 过滤

主界面顶部提供：

- 全部；
- 今天；
- 已逾期。

切换后仍然显示 2×2 四象限，只过滤每个象限中的任务。

定义：

- Today：`DueAt` 的本地日期等于今天；
- Overdue：`DueAt < Now` 且未完成。

### 2.11 搜索

`Ctrl + F` 聚焦搜索框。

V1 搜索字段：

- Title；
- Note。

不做高级查询语法。

### 2.12 已完成

单独的已完成列表：

- 按 `CompletedAt` 倒序；
- 支持恢复；
- 支持永久删除；
- 不做统计图、趋势、成就。

### 2.13 系统托盘

支持：

- 最小化到托盘；
- 双击托盘图标恢复主窗口；
- 右键：新建任务 / 显示主窗口 / 退出；
- 设置项：关闭按钮行为为“最小化到托盘”或“退出”。

### 2.14 开机启动

可选：

- 登录 Windows 后启动；
- 启动后保持后台/托盘，不主动抢焦点。

V1 采用用户级启动机制，不要求管理员权限。

### 2.15 主题

默认：跟随系统 Light/Dark。

V1 设置中可提供：

- 跟随系统；
- 浅色；
- 深色。

优先使用 WPF .NET 10 原生 Fluent Theme 与 `ThemeMode`。

---

## 3. 明确不做的功能

V1 **禁止**实现以下内容，即使实现者认为“顺手就能做”：

- 账号与登录；
- 云同步 / WebDAV / OneDrive 数据同步；
- 网络请求；
- AI；
- 团队协作；
- Project / Workspace；
- 复杂标签体系；
- 子任务树；
- 附件；
- Markdown / 富文本；
- 日历月视图；
- 甘特图；
- 番茄钟；
- Habit；
- 统计报表；
- 插件；
- 自动更新器；
- 移动端；
- 浏览器扩展；
- 自然语言日期解析；
- 重复任务（V1.1 再考虑）。

---

## 4. 关键交互

### 4.1 主窗口

默认尺寸建议：1180 × 760；最小建议：920 × 620。

顶部：

- 应用名；
- All / Today / Overdue；
- 搜索；
- 已完成；
- 设置。

主体：2×2 等宽等高象限。

### 4.2 Task Card

常态只显示：

- 完成圆点/复选交互；
- Title；
- Due 信息；
- Reminder 图标/时间（存在时）；
- 左侧或顶部 3px 象限 Accent。

Hover 时才显示次级操作：编辑、更多。

禁止把整张卡片做成高饱和象限颜色。

### 4.3 Task Editor

字段顺序固定：

1. Title
2. Quadrant
3. Due Date / Time
4. Reminder preset / custom
5. Note
6. Cancel / Save

### 4.4 键盘优先

V1 必须保证：

- `Ctrl + Alt + Q`：Quick Add（默认）；
- `Ctrl + F`：搜索；
- Quick Add 中 `Ctrl + 1..4`：选象限；
- Enter：提交；
- Esc：取消/关闭对话框。

---

## 5. 性能与隐私要求

### 5.1 性能原则

- 无网络；
- 无后台同步；
- 无每秒/每分钟业务轮询；
- 无不必要动画；
- 无 WebView / Chromium / Electron；
- 数据仅在用户操作或系统事件发生时读写；
- 列表使用 WPF 虚拟化；
- 缩到托盘后 UI 不做周期刷新。

### 5.2 目标性指标

这些是工程目标，不是尚未测量前的事实：

- Idle CPU：长时间接近 0%，60 秒采样平均目标 < 0.2%；
- 无应用主动网络连接；
- 1000 个任务时 UI 仍可流畅滚动、过滤、拖拽；
- 启动/托盘恢复不出现明显阻塞；
- 数据写入崩溃后不损坏 SQLite 主库。

内存占用必须在性能阶段实测并记录，不在 SPEC 中伪造固定 MB 数。

### 5.3 数据位置

默认应用数据目录：

`%LOCALAPPDATA%\Quadrant\`

包含：

- `quadrant.db`
- `logs\`（若启用诊断日志）
- 可选配置缓存

不得默认写入用户 Documents。

---

## 6. V1 Release Gate

只有全部满足才能称为 V1：

- 四象限 CRUD 完整；
- 拖拽换象限；
- Due / Reminder 分离；
- Windows 原生通知；
- 完成 / 延后 / 打开通知操作；
- Today / Overdue；
- 搜索；
- 已完成列表；
- Quick Add + 全局热键；
- 托盘；
- 开机启动；
- SQLite 持久化；
- Light/Dark；
- DPI / 键盘 / 基础可访问性通过；
- 性能与崩溃恢复检查通过；
- Release 构建与安装/便携部署说明完成。
